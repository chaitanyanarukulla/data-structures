language: python
python:
  - "2.7"
  - "3.6"

notifications:
  email: false

install:
  - pip install tox-travis
  - pip install coveralls

script:
  - tox

after_success:
  - coveralls

install:
  - pip install -e .
  - pip install -r requirements.txt
script: py.test

